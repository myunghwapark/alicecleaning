<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="style_Spinner">
        <item name="android:background">@drawable/dropdown</item>
        <item name="android:dropDownSelector">@android:color/transparent</item>
        <item name="android:popupBackground">@color/redOrange</item>
        <item name="android:spinnerMode">dropdown</item>
    </style>

    <style name="style_Spinner_Small">
        <item name="android:background">@drawable/dropdown_small</item>
        <item name="android:dropDownSelector">@android:color/transparent</item>
        <item name="android:popupBackground">@color/redOrange</item>
        <item name="android:spinnerMode">dropdown</item>
    </style>
</resources>
